<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/unpressed_blue</item>
        <item name="android:windowExitTransition" tools:targetApi="lollipop">@transition/slide</item>
        <item name="android:windowEnterAnimation">@transition/slide</item>
        <item name="android:windowReenterTransition" tools:targetApi="lollipop">@transition/slide</item>
    </style>
    <style name="AppTheme.FadeTransition" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:activityOpenEnterAnimation" tools:targetApi="lollipop">@transition/fade</item>
        <item name="android:activityCloseExitAnimation">@transition/fade</item>
        <item name="android:windowActivityTransitions" tools:targetApi="lollipop">@transition/fade</item>
        <item name="android:windowReenterTransition" tools:targetApi="lollipop">@transition/fade</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowExitTransition" tools:targetApi="lollipop">@transition/slide</item>
        <item name="android:windowEnterAnimation">@transition/slide</item>
        <item name="android:windowActionBar">@null</item>
        <item name="android:windowReenterTransition" tools:targetApi="lollipop">@transition/slide</item>
    </style>
    <style name="AppTheme.NoActionBar.FadeTransition" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowExitTransition" tools:targetApi="lollipop">@transition/fade</item>
        <item name="android:windowEnterAnimation">@transition/fade</item>
        <item name="android:windowActionBar">@null</item>
        <item name="android:windowReenterTransition" tools:targetApi="lollipop">@transition/fade</item>
    </style>

    <style name="ThemeDialog" parent="android:Theme.Material.Light.Dialog" tools:targetApi="lollipop">
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>

    </style>

    <!-- 自定义弹出层-->
    <style name="anim_ctnote">
        <item name="android:windowEnterAnimation">@anim/ctnote_enter</item>
        <item name="android:windowExitAnimation">@anim/ctnote_out</item>
    </style>

    <style name="BeanDialog" parent="android:Theme.Dialog">
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置背景模糊的透明度-->
        <item name="android:backgroundDimAmount">0.3</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:animation">@android:anim/fade_in</item>
    </style>
</resources>
